---
phase: "01-core-engine"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01"]
files_modified:
  - "src/app/globals.css"
  - "src/components/GlassCard.tsx"
  - "src/components/Button.tsx"
  - "src/components/Input.tsx"
  - "src/components/ExchangeNav.tsx"
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-06

must_haves:
  truths:
    - "body background is #0a0a0f globally"
    - "CSS variable --color-cyan resolves to #00d4ff"
    - "GlassCard renders with backdrop-blur, bg-white/5, border-white/10"
    - "Button scale(1.02) on hover and scale(0.97) on active via Framer Motion"
    - "Button primary variant shows cyan glow ring on hover"
    - "Input shows focus ring in cyan on focus, red border on error state"
    - "ExchangeNav shows Trade / Portfolio / History links"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Complete dark glassmorphism design system replacing cricket theme"
      min_lines: 80
    - path: "src/components/GlassCard.tsx"
      provides: "Glass surface primitive used by all feature components"
      min_lines: 15
    - path: "src/components/Button.tsx"
      provides: "Framer Motion button with variants and micro-interactions"
      min_lines: 45
    - path: "src/components/Input.tsx"
      provides: "Glass input with focus glow and error state"
      min_lines: 35
    - path: "src/components/ExchangeNav.tsx"
      provides: "Bottom nav for Trade / Portfolio / History"
      min_lines: 40
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "--color-cyan, --color-bg, glass utility classes"
      pattern: "var\\(--color-cyan\\)"
    - from: "src/components/Button.tsx"
      to: "OrderForm, auth pages, portfolio actions"
      via: "import Button from '@/components/Button'"
      pattern: "from '@/components/Button'"
---

<objective>
Replace the existing cricket-themed design system with the CoinXI dark glassmorphism theme.
Build GlassCard, Button (with Framer Motion micro-interactions), Input, and ExchangeNav components.
These are the atomic building blocks every other Wave 2 and Wave 3 component depends on.

Purpose: Establish the visual language — #0a0a0f dark background, rgba(255,255,255,0.05) glass
surfaces, #00d4ff cyan accent, JetBrains Mono prices, Space Grotesk headings.
Output: 5 files. Complete design system. Base components ready for composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/aasish/Desktop/DREAM11/coinxi/AGENTS.md
@/Users/aasish/Desktop/DREAM11/.planning/phases/01-core-engine/01-CONTEXT.md

<!-- framer-motion may not be installed. Check package.json first.
     If absent: cd /Users/aasish/Desktop/DREAM11/coinxi && npm install framer-motion
     The existing package.json has: next 16.2.2, react 19.2.4, tailwindcss ^4, zustand ^5 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: globals.css — complete dark glassmorphism design system</name>
  <files>src/app/globals.css</files>
  <action>
First check if framer-motion is in package.json:
```bash
grep "framer-motion" /Users/aasish/Desktop/DREAM11/coinxi/package.json
```
If not found, install it:
```bash
cd /Users/aasish/Desktop/DREAM11/coinxi && npm install framer-motion
```

Read the existing src/app/globals.css then completely replace it with the CoinXI exchange design system. The cricket theme (--color-primary: #a04100, orange gradients, etc.) must be removed entirely.

```css
@import "tailwindcss";

@theme inline {
  /* CoinXI Exchange — Dark Glassmorphism */
  --color-bg: #0a0a0f;
  --color-surface: #0f0f1a;
  --color-surface-high: #14141f;
  --color-surface-elevated: #1a1a2e;

  /* Accent */
  --color-cyan: #00d4ff;
  --color-cyan-dim: rgba(0, 212, 255, 0.15);
  --color-cyan-glow: rgba(0, 212, 255, 0.35);

  /* Status */
  --color-green: #00ff88;
  --color-red: #ff4466;
  --color-green-dim: rgba(0, 255, 136, 0.12);
  --color-red-dim: rgba(255, 68, 102, 0.12);

  /* Text */
  --color-text-primary: #f0f0ff;
  --color-text-secondary: rgba(240, 240, 255, 0.6);
  --color-text-muted: rgba(240, 240, 255, 0.35);

  /* Glass */
  --glass-bg: rgba(255, 255, 255, 0.05);
  --glass-border: rgba(255, 255, 255, 0.1);
  --glass-blur: 16px;

  /* Typography */
  --font-display: 'Space Grotesk', sans-serif;
  --font-body: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}

/* Reset */
*, *::before, *::after {
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}

body {
  background: var(--color-bg);
  color: var(--color-text-primary);
  font-family: var(--font-body), system-ui, sans-serif;
  min-height: 100dvh;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}

/* Typography utilities */
.font-display { font-family: var(--font-display), sans-serif; }
.font-mono    { font-family: var(--font-mono), monospace; }

/* Glass utilities */
.glass {
  background: var(--glass-bg);
  backdrop-filter: blur(var(--glass-blur));
  -webkit-backdrop-filter: blur(var(--glass-blur));
  border: 1px solid var(--glass-border);
}

.glass-elevated {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(255, 255, 255, 0.12);
}

/* Cyan glow helpers */
.glow-cyan {
  box-shadow: 0 0 20px var(--color-cyan-glow), 0 0 40px rgba(0, 212, 255, 0.1);
}

.glow-cyan-sm {
  box-shadow: 0 0 8px var(--color-cyan-dim);
}

.text-cyan  { color: var(--color-cyan); }
.text-green { color: var(--color-green); }
.text-red   { color: var(--color-red); }

/* Price number styling */
.price-up   { color: var(--color-green); }
.price-down { color: var(--color-red); }

/* Scrollbar */
::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.15); border-radius: 2px; }
::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.25); }

/* Thin separator */
.border-glass { border-color: var(--glass-border); }

/* PWA safe areas */
@supports (padding-top: env(safe-area-inset-top)) {
  body {
    padding-top: env(safe-area-inset-top);
    padding-bottom: env(safe-area-inset-bottom);
  }
}

/* Selection */
::selection {
  background: var(--color-cyan-dim);
  color: var(--color-text-primary);
}
```
  </action>
  <verify>
    <automated>grep -c "color-cyan" /Users/aasish/Desktop/DREAM11/coinxi/src/app/globals.css</automated>
  </verify>
  <done>
    - globals.css contains --color-cyan: #00d4ff
    - body background is #0a0a0f
    - .glass utility class exists with backdrop-blur and rgba(255,255,255,0.05)
    - Cricket theme variables (--color-primary: #a04100) are removed
    - framer-motion is present in package.json (installed if it was missing)
  </done>
</task>

<task type="auto">
  <name>Task 2: GlassCard + Button + Input + ExchangeNav components</name>
  <files>
    src/components/GlassCard.tsx
    src/components/Button.tsx
    src/components/Input.tsx
    src/components/ExchangeNav.tsx
  </files>
  <action>
Create all four components. These are new files — read existing files in the directory first to understand import conventions, then create.

**A. src/components/GlassCard.tsx**

```typescript
import { cn } from '@/lib/utils'

interface GlassCardProps {
  children: React.ReactNode
  className?: string
  elevated?: boolean
}

export default function GlassCard({ children, className, elevated = false }: GlassCardProps) {
  return (
    <div
      className={cn(
        'rounded-2xl p-4',
        elevated ? 'glass-elevated' : 'glass',
        className
      )}
    >
      {children}
    </div>
  )
}
```

Note: src/lib/utils.ts already exists (check with `ls src/lib/`). If it does not export `cn`, add:
```typescript
// In src/lib/utils.ts — append if cn is missing:
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
Check if clsx and tailwind-merge are installed. If not, use a simple join instead:
```typescript
// Fallback cn if clsx/tailwind-merge not available:
export function cn(...classes: (string | undefined | false | null)[]) {
  return classes.filter(Boolean).join(' ')
}
```

**B. src/components/Button.tsx**

```typescript
'use client'

import { motion } from 'framer-motion'
import { cn } from '@/lib/utils'

type ButtonVariant = 'primary' | 'secondary' | 'danger' | 'ghost'

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant
  isLoading?: boolean
  children: React.ReactNode
}

const variantStyles: Record<ButtonVariant, string> = {
  primary: [
    'bg-[var(--color-cyan)] text-[#0a0a0f] font-semibold',
    'hover:shadow-[0_0_20px_var(--color-cyan-glow)]',
  ].join(' '),
  secondary: [
    'glass text-[var(--color-text-primary)] font-medium',
    'hover:border-[var(--color-cyan)] hover:text-[var(--color-cyan)]',
  ].join(' '),
  danger: [
    'bg-[var(--color-red)] text-white font-semibold',
    'hover:shadow-[0_0_16px_rgba(255,68,102,0.4)]',
  ].join(' '),
  ghost: 'text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)]',
}

export default function Button({
  variant = 'primary',
  isLoading = false,
  className,
  children,
  disabled,
  ...props
}: ButtonProps) {
  return (
    <motion.button
      whileHover={{ scale: disabled || isLoading ? 1 : 1.02 }}
      whileTap={{ scale: disabled || isLoading ? 1 : 0.97 }}
      transition={{ type: 'spring', stiffness: 400, damping: 25 }}
      className={cn(
        'relative inline-flex items-center justify-center gap-2',
        'rounded-xl px-5 py-2.5 text-sm transition-colors duration-150',
        'disabled:opacity-50 disabled:cursor-not-allowed',
        variantStyles[variant],
        className
      )}
      disabled={disabled || isLoading}
      {...(props as React.ComponentPropsWithoutRef<typeof motion.button>)}
    >
      {isLoading ? (
        <span className="h-4 w-4 rounded-full border-2 border-current border-t-transparent animate-spin" />
      ) : null}
      {children}
    </motion.button>
  )
}
```

**C. src/components/Input.tsx**

```typescript
'use client'

import { cn } from '@/lib/utils'
import { forwardRef } from 'react'

interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label?: string
  error?: string
  prefix?: string
  suffix?: string
}

const Input = forwardRef<HTMLInputElement, InputProps>(
  ({ label, error, prefix, suffix, className, id, ...props }, ref) => {
    const inputId = id || label?.toLowerCase().replace(/\s+/g, '-')
    return (
      <div className="flex flex-col gap-1.5">
        {label && (
          <label
            htmlFor={inputId}
            className="text-xs font-medium text-[var(--color-text-secondary)] uppercase tracking-wide"
          >
            {label}
          </label>
        )}
        <div className="relative flex items-center">
          {prefix && (
            <span className="absolute left-3 text-sm text-[var(--color-text-muted)] font-mono">
              {prefix}
            </span>
          )}
          <input
            ref={ref}
            id={inputId}
            className={cn(
              'w-full rounded-xl glass px-4 py-2.5 text-sm font-mono',
              'text-[var(--color-text-primary)] placeholder:text-[var(--color-text-muted)]',
              'outline-none transition-all duration-150',
              'focus:border-[var(--color-cyan)] focus:shadow-[0_0_0_2px_var(--color-cyan-dim)]',
              error && 'border-[var(--color-red)] focus:border-[var(--color-red)] focus:shadow-[0_0_0_2px_rgba(255,68,102,0.2)]',
              prefix && 'pl-8',
              suffix && 'pr-8',
              className
            )}
            {...props}
          />
          {suffix && (
            <span className="absolute right-3 text-xs text-[var(--color-text-muted)]">
              {suffix}
            </span>
          )}
        </div>
        {error && (
          <p className="text-xs text-[var(--color-red)]">{error}</p>
        )}
      </div>
    )
  }
)

Input.displayName = 'Input'
export default Input
```

**D. src/components/ExchangeNav.tsx**

Read src/components/BottomNav.tsx to understand the existing structure, then create the exchange replacement:

```typescript
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'

const tabs = [
  { name: 'Trade',     path: '/trade',     icon: '⬆⬇' },
  { name: 'Portfolio', path: '/portfolio', icon: '◈' },
  { name: 'History',   path: '/history',   icon: '⊡' },
]

export default function ExchangeNav() {
  const pathname = usePathname()

  return (
    <nav className="md:hidden fixed bottom-0 left-0 w-full z-50 glass border-t border-white/10">
      <div className="flex justify-around items-center h-16 px-4 pb-safe">
        {tabs.map((tab) => {
          const isActive = pathname === tab.path || pathname.startsWith(tab.path + '/')
          return (
            <Link
              key={tab.name}
              href={tab.path}
              className={[
                'flex flex-col items-center justify-center gap-1 px-4 py-2',
                'text-xs font-medium uppercase tracking-wider transition-colors duration-150',
                'active:scale-90',
                isActive
                  ? 'text-[var(--color-cyan)]'
                  : 'text-[var(--color-text-muted)] hover:text-[var(--color-text-secondary)]',
              ].join(' ')}
            >
              <span className="text-base leading-none">{tab.icon}</span>
              <span>{tab.name}</span>
              {isActive && (
                <span className="absolute top-0 left-1/2 -translate-x-1/2 w-8 h-0.5 bg-[var(--color-cyan)] rounded-full" />
              )}
            </Link>
          )
        })}
      </div>
    </nav>
  )
}
```
  </action>
  <verify>
    <automated>ls /Users/aasish/Desktop/DREAM11/coinxi/src/components/GlassCard.tsx /Users/aasish/Desktop/DREAM11/coinxi/src/components/Button.tsx /Users/aasish/Desktop/DREAM11/coinxi/src/components/Input.tsx /Users/aasish/Desktop/DREAM11/coinxi/src/components/ExchangeNav.tsx 2>&1</automated>
  </verify>
  <done>
    - GlassCard.tsx: renders div with glass or glass-elevated className
    - Button.tsx: uses motion.button with whileHover scale(1.02) and whileTap scale(0.97)
    - Button primary variant has cyan background and cyan glow on hover
    - Input.tsx: forwardRef component with error state (red border) and focus state (cyan ring)
    - ExchangeNav.tsx: three links Trade / Portfolio / History with active state in cyan
    - framer-motion is imported correctly in Button.tsx
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client components | All components are client-side UI only; no data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Info Disclosure | ExchangeNav | accept | Navigation links are public knowledge; no sensitive data exposed |
| T-02-02 | Tampering | Button disabled state | accept | disabled prop prevents interaction; server validates all mutations |
</threat_model>

<verification>
After both tasks:

```bash
cd /Users/aasish/Desktop/DREAM11/coinxi

# 1. Design tokens present
grep "color-cyan" src/app/globals.css | head -3

# 2. All components exist
ls src/components/GlassCard.tsx src/components/Button.tsx \
   src/components/Input.tsx src/components/ExchangeNav.tsx

# 3. framer-motion in package.json
grep "framer-motion" package.json

# 4. Button uses whileHover scale
grep "whileHover" src/components/Button.tsx

# 5. No cricket theme remnants
grep "a04100\|ff6b00\|fcf9f8" src/app/globals.css
# Expected: zero results
```
</verification>

<success_criteria>
- globals.css: #0a0a0f background, --color-cyan: #00d4ff, .glass utility, no cricket colors
- framer-motion installed in package.json
- Button: motion.button, whileHover scale 1.02, whileTap scale 0.97, primary/secondary/danger/ghost variants
- Input: forwardRef, focus cyan ring, error red border, prefix/suffix slots
- GlassCard: glass or glass-elevated class, rounded-2xl
- ExchangeNav: Trade / Portfolio / History links, active state in cyan
</success_criteria>

<output>
After completion, create `/Users/aasish/Desktop/DREAM11/.planning/phases/01-core-engine/01-core-engine/01-02-SUMMARY.md` with:
- Components created and their export names
- Whether framer-motion was pre-installed or newly added
- cn() implementation used (clsx/twMerge or simple join)
- Any Tailwind v4 quirks encountered
</output>
