---
phase: 01
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - apps/frontend/src/app/page.tsx
  - apps/frontend/src/app/analyze/page.tsx
  - apps/frontend/src/components/AnalyzeForm.tsx
  - apps/frontend/src/components/ScoreCard.tsx
  - apps/frontend/src/components/HeatmapOverlay.tsx
  - apps/frontend/src/components/NeuroScoreGauge.tsx
  - apps/frontend/src/components/CognitiveLoadMeter.tsx
  - apps/frontend/src/components/HierarchyVisualizer.tsx
  - apps/frontend/src/components/AccessibilityPanel.tsx
  - apps/frontend/src/components/CritiquePanel.tsx
  - apps/frontend/src/components/Recommendations.tsx
  - apps/frontend/src/types/analysis.ts
  - apps/frontend/src/lib/api.ts
  - apps/frontend/tailwind.config.ts
  - apps/frontend/src/app/globals.css
  - apps/frontend/src/app/layout.tsx
  - package.json
autonomous: false
requirements: [F1.1, F1.3, F1.4, F1.7, F1.8, F1.9, F1.10, F1.11, F1.12]
user_setup: []

must_haves:
  truths:
    - "User can input URL and trigger analysis"
    - "Heatmap overlay renders on screenshot"
    - "NeuroScore gauge displays 0-100 score"
    - "Score cards show all 5 component scores"
    - "DesignMind critique text displays"
    - "Recommendations are listed with P0-P3 priorities"
  artifacts:
    - path: "apps/frontend/src/app/page.tsx"
      provides: "Landing page with URL input"
    - path: "apps/frontend/src/app/analyze/page.tsx"
      provides: "Analysis results dashboard"
    - path: "apps/frontend/src/components/HeatmapOverlay.tsx"
      provides: "Canvas heatmap rendering"
      exports: ["HeatmapOverlay"]
    - path: "apps/frontend/src/components/NeuroScoreGauge.tsx"
      provides: "Circular gauge for NeuroScore"
      exports: ["NeuroScoreGauge"]
    - path: "apps/frontend/src/components/AnalyzeForm.tsx"
      provides: "URL input form"
      exports: ["AnalyzeForm"]
  key_links:
    - from: "page.tsx"
      to: "/api/analyze"
      via: "POST via api.ts"
      pattern: "fetch.*\\/api\\/analyze"
    - from: "HeatmapOverlay.tsx"
      to: "screenshot + heatmap data"
      via: "Canvas rendering"
      pattern: "canvas.*drawImage"
    - from: "NeuroScoreGauge.tsx"
      to: "neuroScore.score"
      via: "animated circular progress"
      pattern: "stroke-dashoffset"
---

<objective>
Build the NeuroScan frontend dashboard with NYRADNA cinematic luxury design system. Display heatmap overlay, score gauges, critique, and recommendations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-core-engine/01-RESEARCH.md
@.planning/phases/01-core-engine/01-02-SUMMARY.md
@.planning/STATE.md
@$HOME/.claude/projects/-Users-aasish/memory/feedback_design_direction.md

Design system (from memory):
- Background: #050507 (near-black)
- Glass cards: rgba(255,255,255,0.05) + backdrop-blur(50px) + border rgba(198,191,255,0.25)
- Primary: #c6bfff (soft lavender)
- Fonts: Manrope for headlines, Inter for body
- Letter-spacing: 0.2em-0.5em on headings
- Border radius: 0.125rem default
- Cinematic glow: box-shadow inset 0 0 40px rgba(198,191,255,0.08)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project setup + design system</name>
  <files>
    apps/frontend/src/app/globals.css
    apps/frontend/src/app/layout.tsx
    apps/frontend/tailwind.config.ts
    apps/frontend/src/types/analysis.ts
    apps/frontend/src/lib/api.ts
  </files>
  <read_first>
    apps/frontend/tailwind.config.ts
    apps/frontend/src/app/globals.css
  </read_first>
  <action>
    Set up Next.js frontend with NYRADNA design system:

    1. Ensure Next.js 15 App Router project exists in apps/frontend
    2. Configure tailwind.config.ts with design tokens:
       ```typescript
       colors: {
         background: '#050507',
         surface: '#12121a',
         glass: 'rgba(255,255,255,0.05)',
         primary: '#c6bfff',
         'primary-container': '#8c80ff',
         secondary: '#fface8',
         accent: '#8b7fff',
         success: '#4ade80',
         warning: '#fbbf24',
         error: '#f87171',
         'text-primary': '#f0f0f5',
         'text-secondary': '#8888a0',
       }
       fontFamily: {
         heading: ['Manrope', 'sans-serif'],
         body: ['Inter', 'sans-serif'],
       }
       letterSpacing: {
         wide: '0.2em',
         'ultra-wide': '0.5em',
       }
       ```

    3. Update globals.css:
       - Import Manrope + Inter from Google Fonts
       - Set dark background on body
       - Add glass card utility class
       - Add cinematic glow utility

    4. Create apps/frontend/src/types/analysis.ts:
       - Copy AnalysisResult, NeuroScore, ScoreComponents, Recommendation types from backend

    5. Create apps/frontend/src/lib/api.ts:
       - Export `analyzeUrl(url: string): Promise<AnalysisResult>`
       - POST to /api/analyze with timeout handling
       - Return typed response

    6. Add to package.json deps:
       - @types/canvas-confetti (for future animations)
  </action>
  <verify>
    <automated>cd apps/frontend && npx tailwindcss --config tailwind.config.ts init --dummy 2>&1 | head -3 && echo "PASS: design system configured"</automated>
  </verify>
  <done>
    Next.js app renders with NYRADNA design tokens applied
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing page + AnalyzeForm</name>
  <files>
    apps/frontend/src/app/page.tsx
    apps/frontend/src/components/AnalyzeForm.tsx
  </files>
  <read_first>
    apps/frontend/src/components/AnalyzeForm.tsx
    apps/frontend/src/app/page.tsx
  </read_first>
  <action>
    Create the main landing page with URL input:

    1. apps/frontend/src/components/AnalyzeForm.tsx:
       - Glass card container with backdrop-blur
       - URL input field: large, centered, placeholder "Paste any URL to analyze"
       - Primary button: "Analyze Design" with lavender gradient
       - Loading state: animated spinner, "Capturing page..." text
       - Error state: red border, error message below input
       - Validation: https URL required, show inline error
       - On submit: call api.analyzeUrl(), navigate to /analyze page with result

    2. apps/frontend/src/app/page.tsx:
       - Full-height layout with centered content
       - Top: minimal nav with NeuroScan logo/name
       - Center: Large headline "Analyze Any Design in Seconds"
         - Subtext: "Heatmaps, cognitive load, accessibility, and expert-level critique"
       - Below headline: AnalyzeForm component
       - Below form: 3 glass cards showing feature highlights:
         - "Attention Heatmaps" - visual saliency
         - "Cognitive Load Score" - mental effort analysis
         - "WCAG Accessibility" - compliance checking
       - Footer: minimal, copyright

    3. Typography per NYRADNA:
       - Headlines: Manrope, ultra-wide letter-spacing (0.3em)
       - Subtext: Inter, text-secondary color
       - Labels: uppercase, 10px, wide tracking
  </action>
  <verify>
    <automated>cd apps/frontend && npm run build 2>&1 | grep -E "(error|warning|compiled)" | head -10</automated>
  </verify>
  <done>
    Landing page renders with glass card styling, URL input functional
  </done>
</task>

<task type="auto">
  <name>Task 3: HeatmapOverlay + NeuroScoreGauge components</name>
  <files>
    apps/frontend/src/components/HeatmapOverlay.tsx
    apps/frontend/src/components/NeuroScoreGauge.tsx
    apps/frontend/src/components/ScoreCard.tsx
  </files>
  <read_first>
    apps/frontend/src/components/HeatmapOverlay.tsx
    apps/frontend/src/components/NeuroScoreGauge.tsx
  </read_first>
  <action>
    Create the core visualization components:

    1. apps/frontend/src/components/HeatmapOverlay.tsx:
       - Props: {screenshot: string, heatmapData: number[], width: number, height: number, opacity?: number}
       - Canvas rendering:
         - Draw screenshot as base layer
         - Overlay heatmap using hot/cool color map (red-yellow for hot, blue for cold)
         - Apply gaussian blur for smooth gradient
         - Toggle button to show/hide heatmap
       - Hot colors: High attention = red/orange (#ff4444)
       - Cool colors: Low attention = blue (#4444ff)
       - Controls: opacity slider, toggle heatmap visibility

    2. apps/frontend/src/components/NeuroScoreGauge.tsx:
       - Props: {score: number, size?: 'sm'|'md'|'lg'}
       - Circular SVG gauge with animated fill
       - Score color: 0-40 red, 40-70 yellow, 70-100 green
       - Animated number counter (0 to score over 1s)
       - Label below: "NeuroScore"
       - Glow effect on the gauge ring

    3. apps/frontend/src/components/ScoreCard.tsx:
       - Props: {title: string, score: number, description?: string, icon?: ReactNode}
       - Glass card with score display
       - Mini bar showing score
       - Used for: Attention, Click Probability, Scroll Depth, Hierarchy, Accessibility
       - Hover: subtle lift + glow
  </action>
  <verify>
    <automated>cd apps/frontend && npm run build 2>&1 | grep -E "(error|compiled)" | head -5</automated>
  </verify>
  <done>
    HeatmapOverlay renders canvas with heatmap, NeuroScoreGauge animates score
  </done>
</task>

<task type="auto">
  <name>Task 4: Score breakdown + analysis panels</name>
  <files>
    apps/frontend/src/components/CognitiveLoadMeter.tsx
    apps/frontend/src/components/HierarchyVisualizer.tsx
    apps/frontend/src/components/AccessibilityPanel.tsx
    apps/frontend/src/components/CritiquePanel.tsx
    apps/frontend/src/components/Recommendations.tsx
  </files>
  <read_first>
    apps/frontend/src/components/CognitiveLoadMeter.tsx
    apps/frontend/src/components/HierarchyVisualizer.tsx
  </read_first>
  <action>
    Create analysis detail panels:

    1. apps/frontend/src/components/CognitiveLoadMeter.tsx:
       - Props: {score: number, components: {text: number, visual: number, layout: number}}
       - Vertical meter showing overall cognitive load (higher = more complex = worse)
       - Three mini bars for text/visual/layout breakdown
       - Color: green (0-30), yellow (30-60), red (60-100)
       - Tooltip explaining each component

    2. apps/frontend/src/components/HierarchyVisualizer.tsx:
       - Props: {elements: HierarchyElement[]}
       - Scaled-down page representation
       - Elements shown as rectangles sized by importance
       - Color coded by hierarchy score
       - Hover: show element details (tag, score)

    3. apps/frontend/src/components/AccessibilityPanel.tsx:
       - Props: {score: number, issues: WCAGIssue[]}
       - Expandable panel
       - Summary: X issues found, score
       - Grouped by severity: Critical (red), Serious (orange), Moderate (yellow)
       - Each issue: description + WCAG reference + "Learn more" link
       - Collapsed by default, expand on click

    4. apps/frontend/src/components/CritiquePanel.tsx:
       - Props: {critique: string, recommendations: Recommendation[]}
       - Glass card with "DesignMind Critique" header
       - Critique text in readable format (white space, line-height)
       - P0/P1 issues highlighted in callout boxes
       - Overall tone: authoritative, specific, actionable

    5. apps/frontend/src/components/Recommendations.tsx:
       - Props: {recommendations: Recommendation[]}
       - Tabs/filter: All | P0 | P1 | P2 | P3
       - Each recommendation card:
         - Priority badge (colored: P0=red, P1=orange, P2=yellow, P3=gray)
         - Title + description
         - Predicted lift with range (e.g., "+8-15% clicks")
         - Effort badge (low/medium/high)
       - Sort by priority, then by predicted lift
  </action>
  <verify>
    <automated>cd apps/frontend && npm run build 2>&1 | grep -E "(error|compiled)" | head -5</automated>
  </verify>
  <done>
    All panels render with glass card styling, interactions work
  </done>
</task>

<task type="auto">
  <name>Task 5: Analysis results dashboard page</name>
  <files>
    apps/frontend/src/app/analyze/page.tsx
  </files>
  <read_first>
    apps/frontend/src/app/analyze/page.tsx
  </read_first>
  <action>
    Create the main analysis dashboard page:

    1. apps/frontend/src/app/analyze/page.tsx:
       - Receives analysis result via router state or URL params
       - Loading state: skeleton UI with pulsing cards
       - Layout (responsive grid):
         - Top row: URL display + Analyze New button
         - Left column (60%): HeatmapOverlay + screenshot
         - Right column (40%): NeuroScoreGauge (large) + ScoreCards row
         - Below: Tabs for different analysis views:
           - Overview: CognitiveLoadMeter + HierarchyVisualizer
           - Accessibility: AccessibilityPanel
           - Critique: CritiquePanel + Recommendations
       - Mobile: stacked layout, heatmap first

    2. Error/partial state handling:
       - If analysis failed: show error with retry button
       - If partial: show available results, gray out missing sections
       - Status indicator in header

    3. Animation:
       - Scores animate in when page loads
       - Cards fade in staggered
       - Heatmap renders progressively

    4. Add to apps/frontend/src/app/layout.tsx:
       - Add viewport meta for mobile
       - Set default background color
  </action>
  <verify>
    <automated>cd apps/frontend && npm run build 2>&1 | grep -E "(error|compiled|success)" | head -10</automated>
  </verify>
  <done>
    Dashboard renders with all components, responsive layout works
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify frontend dashboard end-to-end</name>
  <what-built>
    Complete NeuroScan frontend:
    - Landing page with URL input
    - Analysis dashboard with heatmap overlay
    - NeuroScore gauge + 5 score cards
    - Cognitive load meter
    - Accessibility panel
    - DesignMind critique
    - P0-P3 recommendations
  </what-built>
  <how-to-verify>
    1. Run: `cd apps/frontend && npm run dev`
    2. Open: http://localhost:3000
    3. Verify: Glass card styling visible (backdrop blur, lavender accents)
    4. Test: Enter "https://example.com", click Analyze
    5. Verify: Dashboard loads with heatmap, scores, critique
    6. Check: Heatmap overlay toggle works
    7. Check: Recommendation tabs filter correctly
    8. Mobile: Resize browser, verify responsive layout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> backend API | HTTPS, typed responses |
| user uploads | N/A for URL input |
| displayed content | Sanitized from API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | I | XSS via critique text | mitigate | React auto-escapes, use dangerouslySetInnerHTML only for trusted HTML |
| T-01-11 | I | Large screenshot DoS | mitigate | Limit screenshot to 4MB, lazy load images |
| T-01-12 | S | Score manipulation | accept | Client-display only, no security impact |
</threat_model>

<verification>
- All components build without errors
- Landing page renders with glass card styling
- Dashboard displays mock data correctly
- Responsive layout works on mobile (375px width)
</verification>

<success_criteria>
- Landing page: Glass cards with lavender accents, URL input functional
- Dashboard: Heatmap overlay renders, NeuroScore gauge animates
- Score cards: All 5 components display with correct colors
- Critique: Text readable with proper typography
- Recommendations: P0-P3 filter tabs work
- Performance: Dashboard loads <2s after analysis complete
- Design: NYRADNA cinematic luxury feel throughout
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-03-SUMMARY.md`
</output>
